<Page x:Class="MaterialAssetsApp.Pages.MassMovePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Массовое перемещение">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Массовое перемещение карточек"
                   FontSize="22"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Увольняемый сотрудник:" VerticalAlignment="Center"/>
            <ComboBox x:Name="cbOldEmployee"
                      Width="250"
                      Margin="10,0"
                      DisplayMemberPath="FullName"
                      SelectedValuePath="EmployeeID"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Новый держатель:" VerticalAlignment="Center"/>
            <ComboBox x:Name="cbNewEmployee"
                      Width="250"
                      Margin="10,0"
                      DisplayMemberPath="FullName"
                      SelectedValuePath="EmployeeID"/>
        </StackPanel>

        <Button Grid.Row="3"
                Content="Выполнить перемещение"
                Width="220"
                Height="40"
                HorizontalAlignment="Left"
                Click="BtnMassMove_Click"/>
    </Grid>
</Page>
