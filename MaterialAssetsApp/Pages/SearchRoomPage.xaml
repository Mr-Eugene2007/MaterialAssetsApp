<Page x:Class="MaterialAssetsApp.Pages.SearchRoomPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Поиск кабинета">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Поиск кабинета"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- Фильтры -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Номер кабинета -->
            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Номер кабинета"/>
                <TextBox x:Name="txtRoomNumber"/>
            </StackPanel>

            <!-- Подразделение -->
            <StackPanel Grid.Column="1" Margin="0,0,10,0">
                <TextBlock Text="Подразделение"/>
                <ComboBox x:Name="cbDepartment"
                          DisplayMemberPath="DepartmentName"
                          SelectedValuePath="DepartmentID"/>
            </StackPanel>

            <!-- Кнопка -->
            <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                <Button Content="Поиск"
                        Width="120"
                        Height="30"
                        Click="BtnSearch_Click"/>
            </StackPanel>
        </Grid>

        <!-- Результаты -->
        <DataGrid Grid.Row="2"
                  x:Name="dgRooms"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  MouseDoubleClick="dgRooms_MouseDoubleClick"
                  SelectionMode="Single">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding RoomID}" Width="60"/>
                <DataGridTextColumn Header="Номер" Binding="{Binding RoomNumber}" Width="120"/>
                <DataGridTextColumn Header="Подразделение" Binding="{Binding DepartmentName}" Width="200"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Page>
