<Page x:Class="MaterialAssetsApp.Pages.SearchRoomPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Поиск кабинета"
      Loaded="Page_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0"
                   Text="Поиск кабинета"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- Фильтры -->
        <WrapPanel Grid.Row="1" Margin="0,0,0,10">
            <StackPanel Margin="0,0,10,8">
                <TextBlock Text="Номер кабинета" Margin="0,0,0,3"/>
                <TextBox x:Name="txtRoomNumber" Width="180"/>
            </StackPanel>

            <StackPanel Margin="0,0,10,8">
                <TextBlock Text="Подразделение" Margin="0,0,0,3"/>
                <ComboBox x:Name="cbDepartment"
                          Width="220"
                          DisplayMemberPath="DepartmentName"
                          SelectedValuePath="DepartmentID"/>
            </StackPanel>

            <StackPanel VerticalAlignment="Bottom" Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="Экспорт в Excel"
                        Width="140" Height="32"
                        Margin="0,0,10,0"
                        Click="BtnExport_Click"/>
                <Button Content="Поиск"
                        Width="110" Height="32"
                        Margin="0,0,10,0"
                        Click="BtnSearch_Click"/>
                <Button Content="Сбросить"
                        Width="110" Height="32"
                        Click="BtnReset_Click"/>
            </StackPanel>
        </WrapPanel>

        <!-- Результаты -->
        <DataGrid Grid.Row="2"
                  x:Name="dgRooms"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  MouseDoubleClick="dgRooms_MouseDoubleClick"
                  SelectionMode="Single"
                  SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер" Binding="{Binding RoomNumber}" Width="120"/>
                <DataGridTextColumn Header="Подразделение" Binding="{Binding DepartmentName}" Width="250"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>